apiVersion: apps/v1
kind: Deployment
metadata:
  name: kukuk-frontend
  namespace: romeo-dev
spec:
  selector:
    matchLabels:
      app: kukuk-frontend
  template:
    metadata:
      labels:
        app: kukuk-frontend
    spec:
      containers:
        - name: frontend-container
          image: digitalmustermann/kukuk_frontend_dev:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80   
apiVersion: apps/v1
kind: Service
metadata:
  name: kukuk-frontend
  namespace: romeo-dev 
spec:
  type: ClusterIP 
  selector:
    app: kukuk-frontend
  ports:
    - port: 80
      targetPort: 80
      
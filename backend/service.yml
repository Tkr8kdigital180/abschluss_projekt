apiVersion: v1
kind: Service
metadata:
  name: kukuk-backend
  namespace: romeo-dev
spec:
  type: ClusterIP
  selector:
    app: kukuk-backend
  ports:
    - port: 8084
      targetPort: 8080